{
  "name": "@nexnet/worker-relay",
  "version": "0.0.1",
  "type": "module",
  "main": "src/index.ts",
  "scripts": { "test": "bun test", "typecheck": "bunx tsc --noEmit -p tsconfig.json", "dev": "wrangler dev", "deploy": "wrangler deploy" },
  "dependencies": {
    "@nexnet/types": "workspace:*",
    "hono": "^4.12.30"
  },
  "devDependencies": { "@cloudflare/workers-types": "^5.20260717.1", "wrangler": "^3" }
}
